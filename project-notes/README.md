# rice-app — Project Notes

โฟลเดอร์นี้เก็บบริบทของโปรเจกต์แทนระบบ memory ของ Claude — **ผู้ใช้ไม่ต้องการให้บันทึกลง memory
ของ Claude เอง** ให้เก็บไว้ที่นี่แทน แล้วผู้ใช้จะสั่งให้อ่านไฟล์นี้ก่อนเริ่มงานเองเมื่อจำเป็น (ไม่ auto-load)

## โปรเจกต์คืออะไร

PWA คำนวณ/พยากรณ์ที่เกี่ยวข้องกับโครงงานวิทยาศาสตร์ของผู้ใช้เรื่อง **"แบบจำลองคณิตศาสตร์
ดาราศาสตร์ย้อนรอยเพื่อระบุค่าช่วงแสงวิกฤตของพันธุ์ข้าวไวแสงในประเทศไทย"** — ไม่ใช่ผลิตภัณฑ์
สำหรับเกษตรกรทั่วไป เป็นเครื่องมือประกอบโครงงานของผู้ใช้เอง (ไม่มี build step, plain HTML/JS/CSS)

รายงานฉบับเต็มอยู่ที่ `C:\Users\User\Downloads\รายงาน Project (1).pdf` — ถ้าจะแก้ไข/เพิ่มฟีเจอร์ที่
เกี่ยวกับตรรกะการทดลองหรือสูตรคำนวณ ควรอ่านบทที่ 3 (วิธีดำเนินงานวิจัย) และบทที่ 4 (ผลการทดลอง)
ในรายงานนั้นประกอบก่อน

ไฟล์หลักในโปรเจกต์:
- `calc-core.js` — ฟังก์ชันคำนวณ/ยูทิลิตี้ที่ใช้ร่วมกันหลายหน้า (รวม `fetchGPS()`)
- `photoperiod_calculator.html` — คำนวณความยาววัน + เทียบสูตร Cooper/Spencer กับข้อมูลจริง
- `planting_predictor.html` — พยากรณ์วันปลูก/เก็บเกี่ยว
- `reference-data/khonkaen-sun-2026.js` — ข้อมูลความยาววันจริงของขอนแก่นปี 2569 (365 วัน)
  จากสมาคมดาราศาสตร์ไทย ใช้เทียบกับค่าที่คำนวณจากสูตร

## Workflow ที่ใช้

โปรเจกต์นี้ผ่านทีม subagent ส่วนตัวของผู้ใช้ ("Avengers of Direness": designer → engineer →
reviewer → chief-of-staff → secretary ตัดสินใจร่วมกับผู้ใช้เมื่อมีการ escalate) — ดู
`~/.claude/CLAUDE.md` และ `~/.claude/agents/` สำหรับรายละเอียด workflow

## ประวัติการทำงาน

### 2026-07-23 คืน → 2026-07-24 — เพิ่มฟีเจอร์เทียบ 3 ทาง + dedupe ปุ่ม GPS

**Engineer ทำ:**
1. รวม `fetchGPS()` + CSS `.gps-btn` ที่เคยก็อปซ้ำใน `photoperiod_calculator.html` กับ
   `planting_predictor.html` มาไว้ที่เดียวใน `calc-core.js`/`style.css`
2. เพิ่มฟีเจอร์ใหม่ในหน้า photoperiod calculator: คำนวณความยาววันด้วยสูตร Cooper (1969) และ
   Spencer (1971) ทั้งคู่เสมอ แล้วเมื่อละติจูดใกล้ขอนแก่น (16.4333°N ±0.5°) จะโชว์ตารางเทียบ 3 ทาง
   (Cooper / Spencer / ข้อมูลจริง) พร้อมค่าเฉลี่ยและผลต่างเป็นนาที
3. เพิ่มไฟล์ `reference-data/khonkaen-sun-2026.js` และ bump service worker cache v10→v11

**Reviewer ตรวจ:** APPROVE — โค้ด dedupe ถูกต้อง ตรรกะเทียบ 3 ทางถูกต้อง (unit, sign convention,
date matching แบบ MM-DD ไม่ผูกปี, atmospheric refraction convention ตรงกันระหว่างแอปกับข้อมูล
อ้างอิง) มีข้อเสนอแนะเล็กๆ 4 ข้อที่ไม่ต้องแก้ (ข้อความ fallback รวมสองกรณี, `updateLatHint()` ที่เรียกซ้ำ
ซ้อนโดยไม่จำเป็น, caveat เรื่อง leap year ที่กระทบแค่ ~1-2 นาทีถ้าผู้ใช้เปลี่ยนปีข้ามปีอธิกสุรทิน)

**Chief-of-staff escalate:** ประเด็นแถว "ค่าเฉลี่ยทั้งสาม" (`avg = (DLCooper + DLSpencer + realDL) / 3`)
— ตั้งข้อสังเกตว่าการเฉลี่ยค่าจริงรวมกับค่าประมาณอาจทำให้เข้าใจผิดว่าแม่นกว่าค่าจริงเดี่ยวๆ

**Secretary + ผู้ใช้ตัดสินใจ:** **เก็บไว้ตามเดิม ไม่ตัดทิ้ง ไม่แก้ไข** — เหตุผล: นี่คือโครงงานของผู้ใช้เอง
ไม่ใช่เครื่องมือให้เกษตรกรใช้จริง ("เกษตรกรห้ามเข้า") ฟีเจอร์เทียบ 3 ทางอิงตามหัวข้อ Cross-Validation
ในรายงาน (บทที่ 3.4 ขั้นตอนที่ 5 และบทที่ 4.3) แม้สูตรเฉลี่ยที่ใช้ในแอปจะไม่ตรงตัวอักษรกับรายงาน 100%
(รายงานเฉลี่ยค่าจาก 3 ชุดทดลองภาคสนามที่คำนวณจากโมเดล ไม่ใช่เฉลี่ยโมเดลกับข้อมูลจริง) แต่ผู้ใช้ยืนยัน
ว่ามีการปรับปรุง/เพิ่มรูปแบบการคำนวณให้ดีขึ้นกว่ารายงานต้นฉบับโดยตั้งใจ — **ไม่ต้องหยิบประเด็นนี้ขึ้นมา
อีกในอนาคตเว้นแต่ผู้ใช้เปิดเอง**

**Commit:** `685d39c` — "Add Cooper/Spencer vs real-data 3-way comparison to photoperiod
calculator; dedupe shared GPS button logic"

## ข้อควรระวัง/สิ่งที่ยังไม่ทำ

- caveat เรื่อง leap year (จุดที่ 4 ที่ reviewer ตั้งไว้) ยังไม่ได้แก้ — ถ้าจะแก้ค่อยทำตอนมีเวลา ไม่เร่งด่วน
- ยังไม่มีข้อมูลจริงจากแปลงทดลอง (ตัวเลขในตาราง 4.1 ของรายงานเป็นค่าสมมติเชิงทฤษฎี รอข้อมูลจริง
  จากการปลูกจริงช่วง ก.ค.-พ.ย. 2569)
